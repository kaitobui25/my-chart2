services:
  fiinquant-sidecar:
    build:
      context: .
    image: lamlong-chart-fiinquant-sidecar:local
    restart: unless-stopped
    ports:
      - "127.0.0.1:8720:8720"
    env_file:
      - path: ./.env
        required: false
    environment:
      HOST: 0.0.0.0
      PORT: 8720
